## To Do
[x] Audit QDLDLC interface. QDLDLCSolver.m confirmed correct — MEX returns strictly lower tri L + Dinv, MATLAB adds speye(n), solve sequence matches QDLDL_solve, permutation handling correct.
[x] Audit function by function and verify correction. Found 7 issues, fixed 4: adaptive rho interval (time→iteration-count), infinity threshold (1e30→1e26), warm_start preserve iterates, polish Moreau decomposition. 2 design differences documented (scaled termination, duality gap). Bug 5 (cost_scaling) not a bug in scaled_termination mode.
[x] Test codegen path ways. Fixed workspace file placement bug (missing trailing filesep). Added codegen tests for workspace/configure/emosqp file presence and prefix. 3 codegen tests pass.
[x] Update documentation. README.md updated: dual backends, project structure, test commands, codegen options, migration notes.
[x] Check code compilation files. make_osqp.m works (osqp_mex compiles and solves correctly). CMakeLists.txt FetchContent pipeline verified. setupOSQPdevelopmentPath.m fixed to include test/mocks. 114/114 tests pass.
[x] Run through Solver.m again. Deep C-parity audit: checkConvergence rewritten to branch on scaled_termination (false=default, matching C). Unscaled path uses D/Dinv/E/Einv/c/cinv for all residuals and tolerances. cost_scaling (scl.c) applied in dual infeasibility check. OSQP_INFTY_THRESH derived as OSQP_INFTY*MIN_SCALING (was hardcoded 1e26). Primal inf check uses explicit polar recession cone projection matching C's project_polar_reccone. dualInfCheck receives eps*norm_dx matching C's in_reccone. 114/114 tests, 9/9 examples pass.
[x] Check for linting issues using MATLAB mcp. All 41 .m files (src, test, utils, integration, mocks) linted — zero issues found.
[x] Run all examples using script. 9/9 examples pass (adjoint_derivatives, basic_usage, code_generation, easy_lasso_qp, exception_handling, interrupt, osqp_demo, update_matrices, update_vectors).
[x] Run all tests and integration tests. 114/114 unit tests pass, 6/6 backend smoke tests pass, 26/26 integration unit tests pass. Fixed integration test using stale status code (-2 → osqp.constant.OSQP_MAX_ITER_REACHED).
[x] Make corresponding documentation updates. Add code comments. README already comprehensive. Added/verified code comments in Solver.m checkConvergence (scaled/unscaled paths), primalInfCheck, dualInfCheck. todo.md updated with completion notes.
[x] Solver.m:74 seems like it should use the constant.m's OSQP_INFTY rather than a hardcoded value. All references now use osqp.constant.OSQP_INFTY.
[x] Why are there hidden constants in Solver.m when we have a class for storing the OSQP constants in constant.m? Solver.m:68-99. Removed entire properties(Constant,Hidden) block (~35 lines). Added 9 algorithm constants to constant.m (OSQP_INFTY_THRESH, OSQP_RHO_MIN/MAX, OSQP_RHO_EQ_OVER_RHO_INEQ, OSQP_RHO_TOL, OSQP_MIN/MAX_SCALING, OSQP_DIVISION_TOL, OSQP_ADAPTIVE_RHO_MULTIPLE_TERMINATION). Replaced ~65 references across Solver.m. 122/122 tests pass.
[x] Compare Solver.m to OSQP C source again. Created docs/algorithm.md with 14-section line-by-line C↔MATLAB comparison covering: constants, setup, ADMM iteration, convergence checking, adaptive rho, post-loop, solution unscaling, polishing, warm start, update data, design differences, status codes.
[x] Check how the OSQP C source is tested. C uses Catch2 with 15 test categories. Added 8 new tests in test/unit/solver_cparity_tests.m covering gaps: large QP (n=100,m=200), no-active-set polishing, semi-definite P with polishing, update-then-resolve, inconsistent bounds, LP polishing, update/warm_start before setup errors. 122/122 tests pass.
[x] Add code comments to Solver.m to help document what each section of code is doing. Added ~30 descriptive comments across all major sections: ADMM loop (3 steps with C function references), convergence check (primal/dual residuals/tolerances with formulas), infeasibility certificates, adaptive rho, Ruiz equilibration iterations, polishing (active set, reduced KKT, Moreau decomposition).
[x] OSQP_INFTY_THRESH shall be calculated from the OSQP_INFTY constant and the minimum scaling factor, i.e., OSQP_INFTY_THRESH = OSQP_INFTY * OSQP_MIN_SCALING. Figure out the best place to do this in Solver.m so the value is calculated once and not repeatedly calculated. i.e. It's constant until something causes it to change in Solver.m once Solver.n is instantiated. This pattern shall be used related to the rest of the constants used in Solver.m. A good pattern seems on setup, take a constants object. Cache this internal in a private property of Solver.m. Use these private properties during iteration so that constants are not recalculated. The only thing that would change them is at the beginning of the problem setup. Added C_ private property (struct) populated by cacheConstants() at start of setup(). All 20 constants and 2 derived values (INFTY_THRESH = INFTY*MIN_SCALING, DIVISION_TOL = 1/INFTY) cached once. All static methods (checkConvergence, computeNewRho, polishSolution, etc.) accept C parameter. solve() grabs C = obj.C_ once. No osqp.constant lookups during iteration. 122/122 tests pass.
[x] algorithm:34 points out a difference between the C code and the MATLAB implementation. This is not acceptable. It's related to my previous task just above this one. Calculate constants shall be calculated during problem setup and cached in private properties of Solver.m so that during the ADMM iteration loop they are not recalculated repeatedly. Fixed constant.m OSQP_DIVISION_TOL from 1e-10 to 1e-30 matching C definition (1.0/OSQP_INFTY). Updated algorithm.md note accordingly. Solver.m cacheConstants() computes DIVISION_TOL = 1/INFTY from base constant. 122/122 tests pass.
[x] algorithm.md:46 points out that C check convexity via linear system solve. MATLAB code uses a cholesky based check. Cholesky requires P to be positive definite. OSQP can handle semi positive definite problems and thus P does not have to be postive definite. Verify that this check is really feature parity with what the C code does. My hunch is that it is not the same. Confirmed: C uses QDLDL (LDL') factorization and counts positive D entries = n. Cholesky fails on PSD matrices with zero eigenvalues. Replaced with two-step check: (1) chol(P+sigma*I) verifies PD (matching C KKT requirement), (2) isLDLpsd(P) uses MATLAB ldl() to detect indefiniteness via D block eigenvalues — handles PSD correctly. Added isLDLpsd static helper. Updated algorithm.md row. 122/122 tests pass.
[ ] Remove reference to `v1.0.0`. We need a better way to manage version number. Do not hard code and docstrings everywhere.
[ ] 


## To Do Later
[ ] Compile on different OS's. I don't trust that this make script is going to work across all platforms. Focus on Linux and Windows. 2020a to 2026a. 
[ ] 